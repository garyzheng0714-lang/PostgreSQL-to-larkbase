/**
 * 「让 AI 帮你填」固定提示词。
 * 设计要点（见 WORKLOG 2026-06-30 前端重构 §AI 帮填）：
 * - 命令 agent 产出**结构化**输出（单行 postgresql:// URI），而非散文 → 粘回连接串框即可，无需脆弱正则。
 * - 强引导**只读角色**，把密码风险降到最低（与后端只读角色建议咬合）。
 * - 隐私兜底：明确告诉 agent，若用户不愿把密码给 AI，用占位符 <PASSWORD> 代替，由用户连接前自行替换。
 * - 交换单元 = 标准连接 URI（确定性解析），归一到已有的连接串输入框。
 */
export const AI_FILL_PROMPT = `我要把一个 PostgreSQL 数据库同步到飞书多维表格，需要一条【只读】的数据库连接串。

请帮我：
1. 如果你能读到我的本地项目配置（.env / 配置文件 / 环境变量），从中提取连接信息；否则逐项问我要：主机、端口、数据库名、用户名、密码。
2. 强烈建议使用或新建一个【只读角色】（仅 SELECT 权限），不要用管理员账号——这条连接串只用于读取同步。
3. 隐私：如果我不希望把真实密码发给你，就在密码位置填占位符 <PASSWORD>，我会在连接前自己替换。
4. 最终**只输出一行**标准连接串，格式如下，不要任何解释或代码块包裹：

postgresql://用户名:密码@主机:端口/数据库名?sslmode=require

如果数据库不支持 SSL，去掉结尾的 ?sslmode=require。`;
